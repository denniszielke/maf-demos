FOUNDRY_PROJECT_ENDPOINT=https://<your-foundry-project-endpoint>
FOUNDRY_MODEL=gpt-4.1-mini
# Authentication uses DefaultAzureCredential from azure-identity.
# Run `az login` (CLI), set up a managed identity, or configure another
# supported credential method — no service-principal secrets required.
ENTERPRISE_MCP_BASE_URL=http://127.0.0.1:9000
A2A_SERVER_URL=http://127.0.0.1:9100
