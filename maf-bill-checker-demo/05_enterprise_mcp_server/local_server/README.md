# Local enterprise MCP server

Run this:
```bash
cd /home/runner/work/maf-demos/maf-demos/maf-bill-checker-demo/05_enterprise_mcp_server/local_server
python server.py
```

Observe this:
- Startup log confirms static JSON loaded.
- All tools return deterministic responses (no outbound dependencies).
